# Tel_Input Control

The `Tel_Input` control is a `Text_Input` with `type="tel"`.

## Usage

```javascript
const tel_input = new controls.Tel_Input({
    context,
    value: '+1 555 0100'
});
```

## Notes

- Inherits behavior from `Text_Input`.

## Tests

- `test/core/missing_controls.test.js`
