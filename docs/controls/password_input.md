# Password_Input Control

The `Password_Input` control is a `Text_Input` with `type="password"`.

## Usage

```javascript
const password_input = new controls.Password_Input({
    context,
    value: 'secret'
});
```

## Notes

- Inherits behavior from `Text_Input`.

## Tests

- `test/core/missing_controls.test.js`
